services:
  - type: web
    name: beer-die-rankings
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    disk:
      name: beer-die-data
      mountPath: /data
      sizeGB: 1
    envVars:
      - key: BEERDIE_DATA_DIR
        value: /data
